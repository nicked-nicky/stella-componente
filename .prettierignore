# Build output and caches — generated, never hand-edited.
dist/
api-docs/
coverage/
**/playwright/.cache/
playwright-report/
test-results/
blob-report/

# Generated by pnpm; reformatting it would churn the diff and can
# confuse the --frozen-lockfile check in CI.
pnpm-lock.yaml

# The component showcase lives in its own repository and is gitignored
# here. Without this line a local `pnpm format` would reach into a
# checkout that isn't part of this project and rewrite it.
# terra-test/

# Components on pause. They live on disk but are deliberately outside the
# repo, the build and the published package until they are picked back up.
packages/terra/src/molecules/CodeBlock/
packages/terra/src/molecules/Tabs/
packages/terra/src/organisms/Accordion/
packages/terra/src/organisms/Drawer/
packages/terra/src/organisms/Table/
